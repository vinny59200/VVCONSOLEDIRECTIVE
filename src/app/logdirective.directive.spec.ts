import { LogdirectiveDirective } from './logdirective.directive';

describe('LogdirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new LogdirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
